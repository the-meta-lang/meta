.SYNTAX AEXP

AEXP = AS $(AS);

AS = .ID ->("address " *) "=" EX1 ->("store") ";";

EX1 = EX5 $("+" EX5 ->("add") |
            "-" EX5 ->("sub") );

EX5 = .ID  ->("load " *) |
      .NUMBER ->("literal " *) |
      "(" EX1 ")";

.END